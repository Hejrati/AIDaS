# Main AIDaS / AI_ForAIDAS requirements.
#
# This file intentionally does not install the legacy OCT Segmenter wheels.
# Those wheels require Python 3.10 and TensorFlow 2.9; install them with
# `requirements-oct-segmenter-legacy.txt` or
# `environment-oct-segmenter-legacy.yml`.
numpy==2.2.6
scipy==1.15.2
matplotlib==3.10.8
Pillow>=10.0
pyreadr
xarray
torch
torchvision
PySide6==6.11.0
shiboken6==6.11.0
